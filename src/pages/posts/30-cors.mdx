import PostLayout from "../../components/post/PostLayout"

// 여기에 내용 작성

export const meta = {    index: 30,
    title: 'CORS',
    description: 'CORS 정책 / 동작 방식 / HTTP 요청-응답 흐름 / CORS 해결 방법',
    category: '',
    series: '',
    tags: ['Network','CORS'], // # 포함하지 않는 string array
    postedAt: '2022. 02. 13',
}
export default ({children}) => <PostLayout meta={meta}>{children}</PostLayout>