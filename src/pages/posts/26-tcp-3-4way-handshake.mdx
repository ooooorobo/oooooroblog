import PostLayout from "../../components/post/PostLayout"

// 여기에 내용 작성

export const meta = {    index: 26,
    title: 'TCP 3 way handshake & 4 way handshake',
    description: 'TCP - 전송 제어 프로토콜(Transmission Control Protocol) → 인터넷 상에서 데이터를 메시지 형태로 보내기 위해 IP와 함께 사용하는 프로토콜',
    category: '',
    series: '',
    tags: ['Network'], // # 포함하지 않는 string array
    postedAt: '2022. 02. 13.',
}
export default ({children}) => <PostLayout meta={meta}>{children}</PostLayout>